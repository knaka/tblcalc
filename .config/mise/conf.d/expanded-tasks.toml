# Code generated by tasks:expand task. DO NOT EDIT.

[tasks."test"]
description='''Run tests.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_test
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_test "$@"
'''

[tasks."run"]
description='''Run cmd.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_run
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_run "$@"
'''

[tasks."doc"]
description='''Update documentation files.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_doc
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_doc "$@"
'''

[tasks."go"]
description='''Run go cmd.'''
run='''
#!/usr/bin/env bash
# .mise/tasks-project.bash:task_go
pushd "$MISE_PROJECT_ROOT"/".mise" >/dev/null || exit 1
. ./"tasks-project.bash"
popd >/dev/null || exit 1
set -o nounset -o errexit -o pipefail
task_go "$@"
'''
